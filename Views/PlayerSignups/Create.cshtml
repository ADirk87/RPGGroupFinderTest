@model GroupFinderCapstone.Models.PlayerSignup

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(model => model.PlayerID)
    @Html.HiddenFor(model => model.EventID)
    @Html.HiddenFor(model => model.CommentGM)
    @Html.HiddenFor(model => model.IsApproved)
    <div class="form-horizontal">
        <h4>PlayerSignup</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })




       

        

        <div class="form-group">
            @Html.LabelFor(model => model.CommentPlayer, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.TextAreaFor(model => model.CommentPlayer, new { @class = "form-control" } )
                @Html.ValidationMessageFor(model => model.CommentPlayer, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
